<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">#13A694</item>
        <item name="background">@color/background_gray</item>
        <item name="android:textColor">@color/creamy</item>
        <item name="android:colorBackground">@color/background_gray</item>
        <item name="android:listDivider">@color/light_gray</item>
    </style>
</resources>